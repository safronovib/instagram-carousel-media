# Instagram Carousel Media

Public, immutable staging files used by the Codex carousel publisher. Every file placed here is intended for a public Instagram post. The publisher uses commit-pinned raw URLs so Meta can fetch the exact verified JPEG without redirects or expiring links.
